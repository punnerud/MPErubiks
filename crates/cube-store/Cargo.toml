[package]
name = "cube-store"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
mpedb.workspace = true
serde.workspace = true
serde_json.workspace = true

# mpedb 0.1.5+ requires the embedder to supply the `mpedb.mpedb_host_now_ms`
# host import on wasm; tools/patch_mpedb_clock.sh injects it at post-build.
